'use strict';

describe('MQ Router Tests', () => {
  describe('Utilities', () => require('./Utilities')); // eslint-disable-line global-require
  describe('Serializers', () => require('./Serializers')); // eslint-disable-line global-require
});
