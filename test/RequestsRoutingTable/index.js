'use strict';

describe('RequestsRoutingTable', () => {
  require('./Initialization'); // eslint-disable-line global-require
  require('./Close'); // eslint-disable-line global-require
  require('./SetMessagesTimeoutListener'); // eslint-disable-line global-require
  require('./Register'); // eslint-disable-line global-require
  require('./CallById'); // eslint-disable-line global-require
  require('./CheckRequestsQueue'); // eslint-disable-line global-require
});
